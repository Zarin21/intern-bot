# intern-bot
A discord bot that scrapes LinkedIn job postings

This is for my personal use as you know it's hard to keep up with the postings:)

# Setup

```
redis-server
mvn clean install
```

# Run

```
export TOKEN="Add your token here"
export CHANNEL="Add your channel ID here"
mvn spring-boot:run
```